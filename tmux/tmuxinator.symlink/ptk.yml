# ~/.tmuxinator/ptk.yml
# you can make as many tabs as you wish...

project_name: ptk
project_root: ~/code/git/ptk/portal-platform-toolkit-8.7/
rvm: 1.8.7-p357
pre:
tabs:
  - ui:
      layout: even-vertical
      panes:
        - cd src/main/webapp/ui && vim
        - cd src/main/webapp/ui
  - servers:
      layout: even-vertical
      panes:
        - cd src/main/webapp/ui && rm -f .guard-rack-development.pid && bundle exec guard
        -
  - channels:
      layout: even-horizontal
      panes:
        - cd ../i18n/8.7/
        - cd ../product-definitions/
  - root:

