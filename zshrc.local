export PATH="/usr/local/bin:$PATH"
cdpath=($HOME/src)

autoload -U compinit
compinit

fpath=(~/dotfiles-local/zsh/functions $fpath)
fpath=(/usr/local/share/zsh-completions $fpath)

setopt no_list_beep
setopt complete_aliases
unsetopt hist_verify
